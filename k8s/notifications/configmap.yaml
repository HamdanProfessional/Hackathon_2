apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-config
  namespace: production
data:
  smtp-host: "smtp.gmail.com"
  smtp-port: "587"
  smtp-from: "noreply@todoapp.com"
  email-enabled: "false"
  # Note: Set email-enabled to "true" and configure secrets to enable email notifications
