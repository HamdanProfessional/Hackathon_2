# JWT Secret for authentication (generate a secure random string)
# Get this from: Generate using `openssl rand -base64 32` or similar
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database URL for PostgreSQL (Neon or local development)
# For Neon: Get from your Neon console -> Connection string
# For local: postgresql://postgres:password@localhost:5432/todos
DATABASE_URL=postgresql://postgres:password@localhost:5432/todos

# Better Auth Configuration
# Better Auth Secret for JWT signing (should match JWT_SECRET)
BETTER_AUTH_SECRET=your-super-secret-jwt-key-change-this-in-production

# Better Auth URL (URL of the auth server, usually the Next.js app)
# In production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# Next.js Public API URL (Backend URL for the frontend client)
# In production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Additional environment variables
NEXTAUTH_SECRET=your-nextauth-secret