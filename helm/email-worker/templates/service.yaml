apiVersion: v1
kind: Service
metadata:
  name: {{ include "email-worker.fullname" . }}
  labels:
    {{- include "email-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "email-worker.selectorLabels" . | nindent 4 }}
